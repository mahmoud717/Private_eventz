<%= render  "shared/notice" %>

<div class="container">
    <div class="created-eventz">
        <h2> Created eventz </h2>
        <div class="events-list">
            <% @user.events do |event| %>

            <%= link_to  event.name, event_path(event), class: "list-group-item list-group-item-action" %>

            <% end %>
        </div>
    </div>


</div>